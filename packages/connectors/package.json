{
  "name": "@lumaops/connectors",
  "version": "0.0.0",
  "private": true,
  "description": "LumaOps ConnectorAdapter implementations (one folder per provider).",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./github": "./src/github/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@lumaops/core": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
